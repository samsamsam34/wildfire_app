# API Trust Diagnostics

This API supports optional trust metadata on property assessments.

These diagnostics are **not** ground-truth accuracy validation and **not** insurer/carrier approval signals.

## Endpoints

- `POST /risk/assess?include_diagnostics=true`
- `GET /report/{assessment_id}?include_diagnostics=true`

If `include_diagnostics` is omitted or `false`, responses keep the existing assessment shape.

Example request:

```bash
curl -sS -X POST "http://127.0.0.1:8000/risk/assess?include_diagnostics=true" \
  -H "Content-Type: application/json" \
  -d '{
    "address": "123 Main St, Missoula, MT 59801",
    "attributes": {
      "roof_type": "composite",
      "vent_type": "standard",
      "defensible_space_ft": 15
    },
    "confirmed_fields": ["roof_type", "vent_type", "defensible_space_ft"],
    "audience": "homeowner"
  }'
```

## Response Shape (Opt-In)

When diagnostics are requested, the response is wrapped:

```json
{
  "assessment": { "...existing AssessmentResult fields..." },
  "diagnostics": {
    "version": "ngt_eval_v1",
    "evaluation_basis": "no_ground_truth",
    "caveat": "These diagnostics measure model coherence, stability, evidence quality, and external alignment. They do not establish real-world predictive accuracy or insurer approval.",
    "confidence": {},
    "stability": {},
    "mitigation_sensitivity": {},
    "monotonicity": {},
    "benchmark_alignment": {},
    "distribution_context": {},
    "explanations": []
  }
}
```

Default mode example (no diagnostics):

```bash
curl -sS -X POST "http://127.0.0.1:8000/risk/assess" \
  -H "Content-Type: application/json" \
  -d '{ "...same payload..." }'
```

## Field Intent

- `confidence`: evidence completeness and fallback pressure for this specific run.
- `stability`: bounded local perturbation sensitivity (small geocode jitter + fallback-assumption perturbation).
- `mitigation_sensitivity`: lightweight counterfactuals using existing mitigation-relevant inputs.
- `monotonicity`: directional checks over the mitigation counterfactual set.
- `benchmark_alignment`: sanity-check context from precomputed no-ground-truth reference artifacts, if available.
- `distribution_context`: optional percentile/segment context from reference distribution artifacts.

## Offline Reference Artifacts

Optional reference loading is controlled by:

- `WF_TRUST_REFERENCE_ARTIFACT_DIR`

Expected files in that directory (generated by the no-ground-truth pipeline):

- `evaluation_manifest.json`
- `distribution_results.json`
- `benchmark_alignment_results.json`

If artifacts are missing or invalid, diagnostics degrade gracefully:

- `benchmark_alignment.available = false`
- percentile fields become `null`
- response still returns assessment + available diagnostics

## Caveats

- These diagnostics are trust metadata, not claims-performance metrics.
- External alignment is directional sanity checking only.
- Do not interpret this payload as underwriting equivalence or model approval.
